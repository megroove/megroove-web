# PROGRESS.md — 現在地と引き継ぎ

## 最初に読むファイルと順序（新しいセッションはここから）

1. **CLAUDE.md** — 設計憲法。最優先方針・データモデル・世界観・健康情報の規律（§12）。すべての判断の基準
2. **PROGRESS.md**（本書） — 現在地・進行ルール・未解決の小課題
3. **ROADMAP.md** — これからの計画（Phase F 候補・中長期・収益化・四半期運用の型）
4. `docs/brushup-plan.md`・`docs/data-rewards-design.md` — 過去の計画と経緯（参照用）
5. **SPEC.md** — システム構成・利用サービス・費用の全体像（引き継ぎ・非技術者向け説明用。現状 git 未コミット）

## 進行ルール（ユーザーと合意済みの作業サイクル）

**担当は Claude が設計から実装まで一貫して行う。**
（一時「設計担当と実装担当をモデルで分ける」運用を試したが、引き渡しコストが大きく
セッション消費も増えたため 2026-07-17 に廃止。以後この分担は復活させない。）

**1項目ずつ「計画提示 → 合意 → 実装 → ブラウザ確認 → コミット」。**
いきなり実装しない。計画は別モデルに渡す詳細設計書ではなく、ユーザーが判断できる粒度
（何を・なぜ・どこを変えるか、確認ポイント、懸念点、選択肢＋推奨）でよい。
計画時に要点の選択肢を AskUserQuestion で確認するのが定着したリズム。
ビルドは `npm run build`（tsc 込み）、確認は dev サーバー、デプロイは `npm run deploy`（gh-pages）。

## 現在の状態（2026-09-18 時点）

- **記録フォーム UI 改善 P1「いつもの一杯」クイック記録（完了・未デプロイ）**: ホームのクイック記録シートを
  `src/components/brew/QuickBrewSheet.tsx` に切り出し、案B のモックに合わせて拡張。
  プリセット最大3枚（「前回と同じ」＋よく使うレシピ上位2。レシピの実体は"直近でそれを使った記録"なので豆・器具ごと引き継ぐ）／
  「今日だけ変えたところ」チップ（粉量・湯量・湯温・挽き目。記録画面と同じ刻みで `clampTweakValue` によりクランプ）／
  **評価は任意**（星なし＝評価待ちに入る）／保存後は演出（星あり＝フル／星なし＝静か）のあとに
  「記録しました・取り消す」トースト（`deleteBrew` で1件のみ・二重実行しない）。
  就寝時カフェイン予測は新設の `predictBedtimeResidual()` に集約し、記録画面・カフェ版クイックの重複計算も解消。
  DB・バックアップ・`Brew` 型は無変更。テスト基盤として **vitest を導入**（`npm test`・`src/db/helpers.test.ts` 16件）
- **F-4 カフェインの傾向（完了・2026-09-26 本番で実データ確認済み）**: カフェイン画面の就寝時予測の下に
  「あなたの傾向」を常時表示。①直近7日の日別摂取量（棒グラフ＋400mg の目安ライン＋記録のあった日の平均）
  ②時間帯の内訳（朝5-11／昼11-15／夕方15-18／夜18-5）と「15時以降が◯%」。
  **§12 厳守＝数値の提示のみ・解釈/助言/因果の断定なし・推定と個人差と出典（EFSA/食品安全委員会）を明示**。
  記録のある日が3日未満なら「記録が3日分たまると出ます」と静かに伝える（母数の小さい割合は誤解を招くため）。
  集計はコーヒー記録＋カフェ記録＋コーヒー以外の飲料の3つを合算（カフェイン管理にのみ合流・既存方針どおり）。
  ロジックは `src/components/caffeine/trends.ts` の純粋関数に集約しテスト14件。DB・バックアップ変更なし
  - 実装中に**日別と時間帯で集計の窓がずれる不具合**を自テストで検出・修正（時間帯側だけ「現在時刻まで」で
    切っており、今日の夕方以降のぶんが棒グラフには乗るのに割合には乗らない状態だった）
  - **検証は本番で実施**: preview と本番は別オリジン＝IndexedDB も別で、preview には実データが無い。
    傾向のように「貯まった記録」を要する機能は、読み取り専用かつデータ構造を変えない範囲であれば
    本番に出してから実データで確認する方が確実（利用者が本人のみのため取れる判断）
- **F-B 聴いていた曲（完了・2026-09-25 実機確認済み）**: Side B の詳細に曲名・アーティストを記録
  （`Brew.musicTitle` / `musicArtist`）。入力は `OriginInput` の `master={[]}` による履歴オートコンプリート、
  記録詳細に表示、ライブラリ検索の対象。前回値はコピーしない（scene と同じ扱い）。分析には使わず、
  データ提供は allowlist 方式のため**自動的に対象外**。DB v5・バックアップ version 3 据え置き（任意フィールド追加）
  - 入力例（プレースホルダー）は `Take Five` / `The Dave Brubeck Quartet`。**実在の曲名・アーティスト名を
    テキストで例示するのは可**という整理（題号・名称に著作権は及ばない／出所表示としての商標的使用でない／
    パブリシティ権は「専ら顧客吸引力の利用」が要件）。カフェ名マスターの「ロゴ/商標を使わずテキストのみ」と同じ線。
    **ロゴ・ジャケット画像は使わない／推薦や提携を思わせる表現にしない／歌詞は載せない**を守ること
  - **ついでに既存の不具合を1件修正**: `loadBrewLayout()` のブロック補完が**保存済み設定にしか掛かっておらず**、
    既定値に載っていない `scene`（シーン・飲み方）が**新規ユーザーには一生表示されない**状態だった
    （カスタマイズ画面にも出てこないので有効化もできない）。補完を既定値にも掛けるようにし、テストで固定
- **記録フォームUI改善（案B/案A/盤の色）は P1〜P5 すべて完了・デプロイ済み（2026-09-25）**。
  以降の残タスクは ROADMAP §2 の F-4（カフェイン週間トレンド）・F-7残り（「1年前の今日」のフォールバック）等
- **P5 仕上げ（完了・デプロイ済み `48b3b34`）**:
  CLAUDE.md（§3 実装済み・§6 2面構成・§7 ブロックと面・§8 画面構成・§9 盤の色・localStorage 表）と
  ROADMAP（F-5 を完了に、F-A として記録フォームUI改善を履歴に追加）を実装に同期。
  localStorage キーの実コードと憲法の表の突き合わせ・バックアップ/匿名化への影響なし・
  DB v5／バックアップ version 3 据え置きを確認。
  **P3 の副作用を1件修正**: 下書きの「変更あり」判定に `side` が入っており、タブを切り替えただけで
  「入力中だった内容を復元しました」が出る状態だった。面の切り替えと詳細の開閉は**見た目の状態**として
  判定から除外（保存されるときには一緒に残る）
- **P4 盤の色（完了・2026-09-25 動作確認済み）**: 7色（豆に合わせる＝既定・焙煎度から導く /
  クラシック黒 / クリアピンク / ミント / ターコイズ / コバルト / レモン）。**抽出中の画面と保存演出のみ**に適用し、
  ホーム・記録詳細・ツアーの盤は既定色のまま。設定画面と抽出中画面の両方から変更でき `megroove-settings` に保存
  （バックアップ対象外＝決定A）。`AppSettings.vinylColor` は `toVinylColorId()` のホワイトリストを必ず通す。
  レーベルは全色ともコーラル固定でコントラスト 4.5:1 を確保、盤に縁取り＋**回転しない光沢**（`VinylGloss`）を追加。
  新規: `VinylGloss` / `VinylPicker`、`RecordDisk`・`Turntable`・`SaveAnimation` は任意の `vinyl` を受ける
  （**省略時は現行色**なので既存の呼び出しは無改変）。テスト 28 件に
- **P3 案A＝記録フォームの2面化（完了・2026-09-23 iPhone 実機確認済み）**:
  Side A（準備と抽出）/ Side B（味わいと評価）のタブ構成に。state・下書き・プリフィル・保存処理は一切動かさず、
  **ブロックの振り分けと画面の枠だけ**を足した（`BREW_BLOCK_SIDE` で面を固定。ゾーンは各面の中で従来どおり）。
  新規: `BrewingOverlay`（抽出中の全画面。ターンテーブル計測＋蒸らし自動スタート＋Wake Lock。
  「抽出完了」で総抽出時間を確定して Side B へ＝**F-5 の自動転記を達成**）／`Turntable`（盤＋トーンアームを部品化）／
  `ExtractionTimeInput`（`ExtractionStopwatch` を手入力専用に整理・計測は抽出中画面へ一本化）。
  `BloomTimer` に `autoStart` を追加（既定は現行どおり）。下書きに `side` を追加（任意＝古い下書きも動く）。
  DB・バックアップ・`Brew` 型は変更なし
- **P2 の回帰検証で不具合を1件発見・修正（2026-09-20）**: 月間/年間トップ3 に**未評価の記録が混じっていた**。
  `rankBrews()` は並べ替えるだけで未評価を除外せず（`rating ?? 0` で末尾に沈むだけ）、評価済みが3杯に満たない月は
  未評価が3位・2位に並び、評価済み0杯なら未評価が1位になっていた（CLAUDE.md「未評価はランキングから除外」に反する）。
  **表示側ではなく `rankBrews()` 自体**を評価済みのみ返すよう修正（呼び出し3箇所すべてが同じ前提。
  ゴールデンレシピ／豆ごとのベストは結果が変わらないことを確認済み）。あわせて、記録はあるが全部未評価のときの
  空状態を「評価待ちの一杯だけです。星をつけるとここに並びます」に出し分け。**P1 以前からあった問題**だが、
  星なし保存が手軽になったぶん顕在化しやすくなっていた
- **P1・保存失敗の可視化ともに iPhone 実機で確認済み（2026-09-20・未デプロイ）**。
  確認は `npm run preview:https` の `https://192.168.0.12:4173/megroove-web/` で実施し、問題なし
- **保存失敗の可視化＋実機確認を https 化（完了・実機確認済み・未デプロイ）**: 全保存経路で ID生成も含めて try で包み、
  `finally` でボタン状態を戻し、`unhandledrejection` の最終安全網（`App.tsx`）を追加。
  `saveErrorMessage()` に「この接続（http）では保存できません」の分岐。`npm run preview:https` で実機確認できる
  （`HTTPS=1` のときだけ自己署名証明書。本番ビルドへの影響なし）。テストは 21 件に
- **睡眠の主観評価 × カフェイン傾向（完了・コミット済み）**: 補助機能・**オプトイン（既定OFF）**。毎朝ワンタップで3段階
  （よく眠れた/ふつう/あまり）を記録。入力は**アプリ内ポップアップ**（午前5〜11時・1日1回・「あとで」でホームの静かな
  カードに委譲・再表示しない）＋カフェインタブでの手動/遡り入力。分析は**前夜の就寝時推定残留量（目標 `bedtimeTargetMg`
  で二分）× 翌朝評価の平均**を数値のみで提示（各グループ5日以上で表示・§12厳守＝解釈/助言なし・免責明示）。
  新ストア `sleepLogs`（**DB v4→v5**・keyPath=date）／`AppSettings.sleepTrackingEnabled`／バックアップ配線＋サニタイズ／
  設定・カフェインタブにトグル。完全ローカル・データ提供対象外。既定OFFのため既存画面は無改変（v5移行の既存データ保持を検証済み）。
  ※当初「モーダル回避」だったが、ユーザー要望でアプリ内ポップアップ採用（オプトイン・急かさない範囲）に方針更新
- **F-8 カフェ名の選択式入力（完了・デプロイ済み）**: カフェ名入力を汎用 `OriginInput` に統一し、全国チェーンの
  マスター（新規 `src/db/cafes.ts`）＋過去入力履歴から候補提示（ひらがな→カタカナ正規化・履歴優先）。
  `OriginInput` に任意 `onSelect` を追加し、履歴の店を選んだときのみ直近訪問のドリンク情報を補完。ロゴ/商標なし・自由入力可・DB変更なし
- **豆・器具に写真を持たせる（完了・デプロイ済み）**: 任意の写真を撮影/選択して登録（袋/パッケージ・器具）。
  共通部品 `src/components/PhotoField.tsx`（`resizeImage` 800px・Brew と同一方式）を、豆フォーム（ストック＋記録画面のその場追加）・
  器具フォーム（ストック）に配置。表示＝ストック一覧・豆ピッカー・記録画面の豆カード・豆分析ページ（＋ライトボックス）・
  ホームの推しの豆（ジャケット）/お気に入り器具。写真は完全に任意で未登録でも自然。銘柄（ドリップバッグ）も Bean 共通で写真可。
  **DBバージョンは4・バックアップは version 3 のまま据え置き**（任意フィールド追加＝後方互換）。import に写真サニタイズを全ストアへ拡張。
  匿名化は allowlist 方式のため写真は提供対象外（変更不要）。器具保存にも `withSaveTimeout` 安全網を横展開
- **味の評価を後から足せる（完了・デプロイ済み `28e3c8f`）**: 未評価＝「まだ針を落としていない盤」。
  条件だけ先に保存（星なしOK）→ 飲んでからホームの「針を落とす一杯」ピル or 記録詳細で後付け評価。
  後付けは星に加え、折りたたみ「詳しく評価する」でカッピング・フレーバーも入力可（既定は閉じ＝手軽さ維持）。
  演出の出し分け: 星あり保存/後付け＝針を落とすフル演出（約2秒・タップでスキップ可）／未評価保存＝盤を置く静かな演出（約2.4秒）。
  ライブラリに「評価待ち」フィルタ＋レコード表示の「未再生」。未評価は分析/ランキング/レーダーから元々除外・杯数には計上（改修不要）
- **豆追加が無言で固まる不具合を修正（コミット `97c9713`・デプロイ済み）**: F-9 の DB v3→v4 化以降、別タブ／古いPWAが
  旧バージョンのDB接続を握るとオープンがブロックされ全書き込みが無言ハング。`openDB` に blocked/blocking/terminated
  ハンドラ＋保存の8秒タイムアウト安全網（`withSaveTimeout`）＋原因別の案内文＋記録画面の豆追加の欠落していた例外処理を追加
- **Phase F ほぼ完了・すべてデプロイ済み**:
  F-1（豆の飲み頃分析）・F-2（ゴールデンレシピカード）・F-3（ストップウォッチのターンテーブル化）・
  F-6（また、あのカフェの一杯）・F-7（日付入力の視認性）・**F-9（コーヒー以外のカフェイン飲料の記録）** が完了。
  残り: F-4（カフェイン週間トレンド・時間帯分布）・F-5（蒸らしタイマー値の自動転記）・
  F-7残り（「1年前の今日」フォールバック検討）
- **記録体験の改善バッチ（完了・デプロイ済み）**:
  - 器具の複数選択＋種類分類（`equipmentIds`。旧 `equipmentId` は読取互換で残置）
  - 抽出方法にドリップバッグを追加（`method='drip_bag'`）。粉量・挽き目・レシピ欄を隠し保存もしない（豆残量に非加算）。
    豆は実態が「銘柄」なので任意化（＝豆必須ガードの唯一の例外）。カフェイン量は代表量 `DRIP_BAG_DOSE_G`(8g) で推定
  - ライブラリ／ホームの「豆の記録なし」を、drip_bag のときは「銘柄なし」に出し分け
  - 粉量・湯量を数値直接入力可に（`Stepper` に文字列バッファ。小数入力を壊さない）
  - 入力途中の下書き自動保存（`megroove-brew-draft`）＋復元通知＋「破棄して最初から」、豆カードの✕クリア、`ErrorBoundary` 追加
- **UI 土台の修正（完了・デプロイ済み）**: app-shell 化でフッター(BottomNav)を fixed から通常フロー最下段へ・
  `html,body` を `overflow:hidden`＋`overscroll-behavior:none` にしてヘッダー／ドラッグ時の固まりを解消・
  ライブラリ／ストックの内部スクロール（末尾が見られない不具合）修正・PWA セーフエリア（上端・左右）対応
- **バックアップ促進（完了・デプロイ済み）**: 初回周知カード＋複合条件リマインダー＋ホームからのその場エクスポート
- **DB は version 4**: `caffeineIntakes` ストア（コーヒー以外のカフェイン飲料ログ、byConsumedAt インデックス）を追加（F-9）
- **ドキュメント**: ROADMAP に §6 公開後のグロース戦略、短期候補に F-8/F-9 を追記。SPEC.md（システム構成・費用）を新規作成（未コミット）
- **Phase A〜E すべて完了・本番デプロイ済み**（全7項目。健康情報の免責明文化 / 1年前の今日 / 前回と同じ一杯 /
  針を落とす保存演出 / 入力支援横展開＋フレーバー頻度順 / 今日の摂取量カード＋就寝時予測 / シーン・飲み方チップ＋デカフェ）
- CLAUDE.md はデータモデル・ブロック一覧・§12 健康方針まで実態と同期済み

### 誤解しやすい点の明確化（過去セッションで確認済みの事実）

- **5-1「1年前の今日」は完了・デプロイ済み。カフェ記録も対象に含めて実装済み**
  （ブリュー＋カフェを横断して±3日探索、`HomePage.tsx` の `findOnThisDay`）。保留ではない
- **「淹れる」画面の日時編集欄は存在する**（画面最上部、Phase A で追加）。
  一時「見当たらない」と報告されたが、原因は旧本番（Phase A 以前）を閲覧していたことで、解決済み

## 未解決の小課題・設計論点

1. 「1年前の今日」の 6ヶ月/1ヶ月フォールバック検討（記録歴1年未満のユーザー向け）。ROADMAP F-7
2. SPEC.md が git 未コミット（システム説明用の新規ドキュメント）。コミットするかは要判断
### 実機確認は https で行う（http の LAN IP は「安全なコンテキスト」ではない・2026-09-18 決着）

**iPhone 実機の確認は `npm run preview:https` を使う。** `npm run preview -- --host` の
`http://192.168.x.x:4173/` で開くと**保存が一切できない**（Mac の localhost は例外的に安全なコンテキスト扱い
なので再現しない＝環境差に注意）。`crypto.randomUUID()` は仕様上 `[SecureContext]` 限定で、http の LAN IP
では `undefined`。`newId()` がここで TypeError になり、保存に到達しない。
- 手順: `npm run preview:https` → **`https://$(scutil --get LocalHostName).local:4173/megroove-web/`** を iPhone で開く →
  自己署名証明書の警告は「詳細を表示」→「このWebサイトを閲覧」で進む（以後そのオリジンは安全なコンテキスト）
- **URL は IP ではなく Bonjour 名（`〜.local`）を使う。** ルーターの DHCP リースが短く、この Mac の IP は
  数日で振り替わる（実際に 192.168.0.12 → .16 → .6 と変わり、古い IP を案内して「アクセスできません」になった）。
  `.local` 名なら IP が変わってもそのまま繋がる（ホスト名が変わると証明書の承認だけやり直し）
- 仕組み: `vite.config.ts` が `HTTPS=1` のときだけ `@vitejs/plugin-basic-ssl` を足す（**本番ビルドに影響しない**）
- 同じく安全なコンテキスト限定: `crypto.subtle`（`src/provision/index.ts` の仮名ID＝データ提供の準備）、
  `navigator.storage.persist()`（App.tsx・任意呼び出しなので実害なし）、Service Worker＝PWA 登録
- 影響を受けない: `crypto.getRandomValues()`（`userSecret` 生成・バックアップの書き出し）、
  インポート（ID はファイル内のものを再利用するため `newId()` を使わない）、写真の撮影/選択（file input）
- **本番（GitHub Pages＝https）では起きない。** ID生成のフォールバックは入れない判断
  （`crypto.randomUUID` は Safari 15.4 以降で利用可能。自前 UUID を持つ責任を負うより、確認を https に寄せる）
- PWA インストール・オフライン動作まで実機で見るときは、自己署名だと Service Worker の挙動が
  ブラウザに依存する。厳密に見たい場合は mkcert でルートCAを iPhone に入れるか、本番デプロイ後に確認する

### 解決済み（経緯の記録）

- **保存が無反応になる（エラーがユーザーに見えない）→ 2026-09-20 に全保存経路を是正**（上の実機確認メモが発端）。
  `setSaving(true)` の後、`try` の外で例外が起きるとボタンが「保存中...」のまま何も出ない状態だった。
  ①ID生成（`newId()`）を含めてハンドラ全体を try で包む ②`finally` でボタン状態を必ず戻す
  ③`saveErrorMessage()` に「安全でないコンテキスト（http）」の分岐を足す ④`unhandledrejection` を
  `App.tsx` の `UnhandledRejectionNotice` で拾ってトースト、の4点を実施。あわせて
  `stock/RecipeTab`・`brew/RecipePickerModal`（try/catch が無かった）に安全網を追加し、
  記録/カフェ記録/カフェイン/後付け評価/睡眠/クイック記録の保存も `withSaveTimeout` ＋ `saveErrorMessage` に統一
  （CLAUDE.md「保存系は同じ安全網に倣う」の実装漏れを解消）
- `datetime-local` の視認性 → F-7 で `color-scheme: dark` を宣言して解決（コミット `1065392`）
- 「また、あのカフェの一杯」の設計論点 → F-6 で決着: 「前回」= 直近のカフェ記録（頻度最多ではない）、
  コピー範囲は `fillFromVisit` と同一（価格含む・評価/カッピング/メモ/シーン/写真は除く）、
  シートは星のみ、保存後はトースト（SaveAnimation は使わない）

## 開発環境のメモ

- dev サーバーのポートは**起動ごとに変わる**（他プロジェクトの Vite が 5173〜 を先に使っていることがある）。
  **`npm run dev` 起動時に Vite が表示する `Local:` の URL を正とする**こと。
  開いた先が Megroove か不安なら `<title>Megroove</title>` で確認（過去に別ポートの別プロジェクトを開いて混乱した実績あり）
- **dev で CSS が消えたら CSP を疑う**: 本番 CSP は `style-src 'self'`。Vite dev は CSS を `<style>` 注入
  するため、`vite.config.ts` の `relaxCspForDev` プラグインが dev 限定で `'unsafe-inline'` を足している
- localhost と本番は別オリジン = **IndexedDB は共有されない**。dev での動作確認にはテストデータを作るか
  本番からエクスポート→インポート
- **実機（iPhone）確認は `npm run preview:https`**（http の LAN IP では保存できない。上記「実機確認は https で行う」参照）
- デプロイ: `npm run deploy`（build + gh-pages publish）。あわせて `git push origin main` も忘れずに
  （過去に main 未プッシュのまま本番だけ古い状態が続き、確認の混乱を招いた）
- PWA は autoUpdate。デプロイ後、既存ユーザーには次回アクセス時に反映（リロード2回で確実）

## 次の一手

- **記録フォーム UI 改善（F-A）は P1〜P5 すべて完了・デプロイ済み**（2026-09-25）。CLAUDE.md / ROADMAP も同期済み
- ROADMAP §2 短期の残りは **F-7（「1年前の今日」の6ヶ月・1ヶ月フォールバック検討）** のみ。
  それ以降は ROADMAP §3 以降（中長期・収益化）か、§4 の四半期トレンド追従サイクルを回して候補を更新する。
  従来どおり「1項目ずつ 計画→合意→実装→ブラウザ確認→コミット」で進める
- ROADMAP.md §2 短期の残り（F-4 カフェイン週間トレンド / F-5 蒸らしタイマー自動転記 / F-7残り）から1項目ずつ。
- SPEC.md のコミット可否は保留中（現状 git 未追跡）。

### 決着済み（味の評価の後付け・実装完了）

「評価待ち」の見せ場所は **ライブラリ主＋ホーム最小ピル（両方）**、対象は **ブリューのみ**（カフェは現状維持）で確定・実装。
保存は単一ボタン維持（星未入力のときだけ控えめな一言）。後付けは星＋（折りたたみで）カッピング・フレーバー。
演出はフル（針を落とす・約2秒・タップでスキップ可）／静か（未評価保存・約2.4秒）の出し分け。件数バッジ・赤丸は不使用。

---

## 記録フォーム UI 改善（案B クイック記録 / 案A Side A・Side B / 盤の色）— 調査結果と計画（2026-09-18・**合意待ち**）

モック: `docs/mock/quick-record-B.mock.html`（案B）・`docs/mock/brew-form-A.mock.html`（案A）。
独自形式のため**仕様として読み、コードは流用しない**（`<x-dc>`＝見た目、末尾 `renderVals()`＝振る舞い）。

### 0. 調査結果（重要：依頼内容のうち「すでに実装済み」のもの）

- **案Bの骨格はすでにある**: ホームの「前回と同じ一杯」クイック記録シート（`HomePage.tsx` 1084-1150、保存 `handleQuickSave` 421-456）。
  就寝時カフェイン予測の1行（`quickPrediction` 528-539）と「詳しく記録する →」も実装済み。
  **不足しているのはモックとの差分4点**: ①プリセットが「直近1件」のみ（よく使うレシピの選択肢がない）
  ②粉量/湯量/湯温/挽き目を「今日だけ変える」チップがない ③**星が未入力だと保存ボタンが無効＝評価必須**
  ④保存後は演出のみで「取り消す」トーストがない
- **「評価待ち」は完了済み**: `Brew.rating` は元から optional。ライブラリの `RatingFilter='pending'`（`LibraryPage.tsx` 269-315）、
  レコード表示の「未再生」、ホームの「針を落とす一杯」、分析・レーダー・トップ3・ベスト条件・豆分析は `rating` 必須で自動除外
  （`stats.ts` 17/25/58/117/146/198）、バックアップは Brew をそのまま入出力＝対応不要。
  → **P2 は新規実装ではなく「クイックシートの星必須を外す＋回帰検証」に縮小できる**
- **カフェイン推定の置き場所**: `estimateCaffeine(doseG, decaf)`・`calcResidualCaffeine(intakes, at)`・`getBedtimeDate()`（`src/db/helpers.ts` 395-415）。
  就寝時予測の組み立ては現在 **3箇所に重複**（`BrewPage.tsx` 168-190 / `HomePage.tsx` 528-539 / 541-552）。
  新UIでは共通化した `predictBedtimeResidual()` を helpers に置き、**4箇所目を作らない**
- **タイマーの実態**: `BloomTimer` は**蒸らしのカウントダウン（既定30秒・Wake Lock・バイブ）で props もコールバックも無い**。
  総抽出時間の計測は別物の `ExtractionStopwatch`（ターンテーブル＝`RecordDisk`＋トーンアーム、停止で `totalTimeSec` 確定）。
  モック案Aの抽出画面（経過時間＋蒸らし→1投目→…の手順ガイド）は**後者に近い**。→ 要判断1
- **盤の色**: `RecordDisk` は色固定の SVG（盤 `#1a0a05`／レーベル `#993C1D`）。使用は5ファイル6箇所
  （ExtractionStopwatch / SaveAnimation×2 / HomePage×2 / BrewDetailPage / OnboardingTour）
- **記録画面カスタマイズとの衝突**: `BrewLayoutSettings { main, detail, hidden }`（localStorage `megroove-brew-layout`）は
  「メイン/詳細/非表示」の3ゾーン。Side A/B は**別軸の概念**なので、面とゾーンの関係を先に決める必要がある。→ 要判断2

### 1. 変更対象ファイルと方針

| ファイル | フェーズ | 方針 |
|---|---|---|
| `src/components/brew/QuickBrewSheet.tsx`（新規） | P1 | `HomePage` のクイック記録シートを**切り出してから**拡張（HomePage は 1487行で最大。これ以上肥大化させない） |
| `src/pages/HomePage.tsx` | P1 | シートを新コンポーネントへ委譲。保存・取り消し・演出の制御のみ残す。カフェ版クイックは**無改変** |
| `src/db/helpers.ts` | P1/P4 | `calcFrequentRecipes()`（`recipeId` 出現頻度）・`predictBedtimeResidual()`（就寝時予測の共通化）・`VINYL_COLORS` と `AppSettings.vinylColor` を追加 |
| `src/pages/BrewPage.tsx` | P3 | Side A / Side B の2面化。ブロック描画（`renderBlock`）と保存ロジックは温存し、**振り分けと画面枠だけ**を足す |
| `src/components/brew/BrewingOverlay.tsx`（新規） | P3 | 「針を落として抽出スタート」後の抽出中画面（ターンテーブル＋蒸らしカウントダウン＋手順ガイド）。完了で `totalTimeSec` を確定して Side B へ |
| `src/components/brew/BloomTimer.tsx` | P3 | `targetSec` / `autoStart` / `onDone` を props で受けられるようにする（既存の抽出ブロックでの使い方は既定値で維持） |
| `src/components/brew/ExtractionStopwatch.tsx` | P3/P4 | 抽出中表示を `BrewingOverlay` と共用できる形に整理。盤の色を反映 |
| `src/components/brew/RecordDisk.tsx` | P4 | 色を props 化（既定＝現状色）。**縁取り**と**回転しない光沢レイヤー**を追加 |
| `src/components/brew/VinylPicker.tsx`（新規） | P4 | 7色の選択UI。抽出中画面と設定画面で共用 |
| `src/pages/SettingsPage.tsx` | P4 | 「機能」セクションに盤の色（`VinylPicker`）を追加 |
| `src/components/brew/SaveAnimation.tsx` | P4 | 盤の色を反映（適用範囲は要判断3） |
| `src/pages/LibraryPage.tsx` | P2 | **変更なし（検証のみ）**。「評価待ち」は実装済み |
| `CLAUDE.md` / `ROADMAP.md` / `PROGRESS.md` | P5 | §6 記録画面・§7 カスタマイズ・localStorage 表・F-5 の扱いを実装に同期 |

### 2. データモデルの変更案とマイグレーション方針

- **IndexedDB のスキーマ変更なし（v5 のまま）**。`Brew` 型の変更なし。**バックアップは version 3 据え置き**（新規フィールドを持たないため）
- 追加するのは**設定のみ**: `AppSettings.vinylColor?: VinylColorId`（localStorage `megroove-settings`）。
  `loadSettings()` は `{...DEFAULT_SETTINGS, ...JSON.parse(raw)}` なので**既存ユーザーは自動で既定値**＝**マイグレーション不要**。
  不正値はホワイトリスト照合で既定へフォールバック
- 色の選択肢（モック `vinylData` 準拠）: `bean`（豆に合わせる・初期値）/ `black` / `pink` / `mint` / `turquoise` / `cobalt` / `lemon`。
  「豆に合わせる」は**焙煎度からの導出**（浅→明るい茶〜深→暗い茶）とする。豆写真からの色抽出は重く不確実なので採らない
- **評価を未入力（null）にした場合の影響＝新たな対応は不要**（§0 のとおり全経路で対応済み）。
  ただし P5 で以下を回帰確認項目として実施: レーダー／トップ3／ベスト条件／豆分析／ライブラリ★フィルタ／エクスポート→インポート往復
- 注意点: `megroove-settings` は**バックアップに含まれない**（就寝時間・目標mg も同様）。盤の色も同じ扱いになる＝機種変更で戻らない。→ 補足質問A

### 3. フェーズ分割と完了条件

**P1 案B（クイック記録シートの拡張）— ✅ 完了（2026-09-18 実装 / 2026-09-20 iPhone 実機確認済み・未デプロイ）**
- プリセット行 = 「前回と同じ」＋よく使うレシピ上位2（最大3枚）。レシピ未使用のユーザーは従来どおり1枚のみ（退行させない）
- 変更チップ = 粉量 / 湯量 / 湯温 / 挽き目。タップで開いて −/＋、変更済みは枠をハイライト。**範囲とステップは BrewPage の Stepper に合わせる**（モックの 8-30 等は採らない）
- 評価は任意化（星なしで保存可・文言は BrewPage と同じ「飲んでから、あとで評価を足せます」）
- 保存後に「記録しました／取り消す」トースト（既存 `Toast` の `action`・5秒）。取り消しは直前に作った id のみ `deleteBrew`
- 完了条件: ホームから2タップで前回と同じ一杯が記録できる／プリセット切替と変更チップが効く／星なし保存が「評価待ち」に入る／取り消しで記録が消える／`npm run build` 通過／カフェ版クイックとドリップバッグ経路が無改変

**P2 評価待ちの仕上げと検証（縮小フェーズ）— ✅ 完了（2026-09-21 目視確認済み・デプロイ済み）**
- コード確認で「未評価が集計に混じらない」ことを全経路で検査し、**トップ3 の1件のみ不具合＝修正済み**。
  問題なしを確認した箇所: レーダー（`stats.ts:17`）／ベスト条件（`:117`）／豆ごとの平均（`:146-154`・杯数には計上）／
  月別トレンド（`:88-91`・同）／飲み頃分析（`:198`）／ホーム今月のベスト（`calcBestDrink`）／豆分析ページ／
  産地パスポート（杯数に計上＝仕様どおり）／バックアップ（`Brew` をそのまま入出力）
- 目視6点（ライブラリ「評価待ち」・レコード表示「未再生」・ホーム「針を落とす一杯」・分析への非影響・
  今日の杯数には計上・エクスポート→インポート往復）も 2026-09-21 に確認し、問題なし
- 実装は P1 の星必須解除のみ。残りは回帰検証（分析・レーダー・トップ3・ベスト条件・豆分析に未評価が出ない／ライブラリ「評価待ち」に出る／ホーム「針を落とす一杯」に出る／バックアップ往復で rating 欠落レコードが保持される）
- 完了条件: 上記6点を実データ（テスト記録）で確認できたこと

**P3 案A（記録フォームの2面化）— ✅ 完了（2026-09-22 実装 / 2026-09-23 iPhone 実機確認済み）**
- Side A（準備と抽出）＝豆・抽出方法・レシピ・粉量/湯量（間に比率の盤表示）・挽き目/湯温・器具・抽出・就寝時カフェイン1行
- Side B（味わいと評価）＝★・フレーバー・シーン／カッピング・メモ・写真は折りたたみ
- 「針を落として抽出スタート」→ 抽出中画面 → 完了で `totalTimeSec` を確定して Side B へ（**F-5 蒸らしタイマー値の自動転記を同時に満たす**）
- 完了条件: 既存ブロックがすべてどちらかの面に出る（非表示は従来どおり）／下書き `megroove-brew-draft` が面をまたいで失われない／編集モード（`/brew/edit/:id`）で破綻しない／豆必須ガードとドリップバッグの出し分けが維持される／タイマーを使わない導線が残っている

**P4 盤の色 — ✅ 完了（2026-09-24 実装 / 2026-09-25 動作確認済み）**
- 7色・既定「豆に合わせる」。抽出中画面と設定画面の両方から変更でき `megroove-settings` に保存
- 盤に縁取り＋**回転しない**光沢レイヤーを追加して背景（`#1a0a05`）と区別
- 完了条件: 7色が選べて再訪後も保持される／明るい盤でもレーベル文字のコントラスト 4.5:1 以上／`prefers-reduced-motion` で回転しない／既存6箇所の盤が壊れない

**P5 仕上げとテスト — ✅ 完了（2026-09-25・デプロイ済み）**
※ 実機リグレッション6点（下書き／節目演出／カフェ版クイック／ドリップバッグ／評価待ち／カフェイン予測）と
  エクスポート→インポート往復のチェックリストは利用者へ引き渡し済み。結果の報告があればここに追記する
- 完了条件: CLAUDE.md（§6・§7・localStorage 表）と ROADMAP/PROGRESS を実装に同期／`npm run build` 通過／iOS Safari（PWA）で実機確認／エクスポート→インポート往復／リグレッション一覧（下書き・節目演出・カフェ版クイック・ドリップバッグ・評価待ち・カフェイン予測）を確認

### 4. 観点別の懸念

**PLANNER**
- 2面化は §2 の最優先「記録の手軽さ」と衝突しうる。1画面で完結していたものが2面になり、**星だけ付けて保存する人の往復が増える**。
  Side A にも保存導線を残すか（モックは Side B のみ）は要判断2の一部
- 既存ユーザーが `main` に昇格させたブロック配置を壊さない前方互換が必要
- 「よく使うレシピ」はレシピ未登録ユーザーには何も増えない（体験差）。初期ユーザー向けには従来表示のまま
- スコープが大きい。P1 単独でも価値が出る順序にしてある（P1 → P2 は独立して出荷可能）

**DEV**
- `HomePage.tsx`（1487行）と `BrewPage.tsx`（845行）が最大級。**先に切り出し（QuickBrewSheet / BrewingOverlay）を行ってから機能追加**する
- 就寝時予測の重複3箇所を helpers に集約してから使う（重複計算を増やさない）
- `BloomTimer` は状態を内部に閉じている。props 追加時も**既存の抽出ブロックの使い方を既定値で維持**する
- 下書き（`saveBrewDraft`）に「いまどちらの面か」を含めるかを決める（含めると復帰位置が自然）
- `Stepper` は文字列バッファ方式のため空文字・不正入力が入りうる → 保存前クランプが必要（SECURITY と同根）
- 取り消しの `deleteBrew` は演出中の二重実行を防ぐ（保存した id を1回だけ・実行後は無効化）
- `RecordDisk` の色 props 化は6箇所に影響。**既定値を現状色にして呼び出し側を無改変で通す**
- 光沢は回転レイヤーと分ける（`disk-spin` は `index.css`。回すと安っぽくなり reduced-motion 対応も崩れる）

**DESIGNER**
- コントラスト 4.5:1: 明るい盤（クリアピンク／ミント／レモン）では `#F7EFE6` のレーベル文字が読めない →
  **盤ごとにレーベル色と文字色をセットで定義**（暗い盤＝クリーム文字／明るい盤＝ダークブラウン文字）
- タップ領域 44px: 色チップ・変更チップ・±ボタン（現在の Stepper は 36px＝`w-9 h-9`）。ヒット領域を余白で 44px 確保
- Side A/B タブは選択状態を色だけでなく形・文言でも示す（色覚多様性）
- 演出の総時間を伸ばさない（フル演出 約2秒枠・タップでスキップ可を維持）。トーストは演出の**後**に出す
- 盤の色は世界観（ダークブラウン基調）の中で浮かないよう彩度を抑えて適用する

**SECURITY**
- 入力値: チップ／ステッパーの値は保存前に **min/max クランプ＋NaN 弾き**（現状は UI 側の下限のみ）
- `localStorage` 由来の設定は破損・手書き改変に備え、`vinylColor` はホワイトリスト照合。`loadSettings` の try/catch＋既定マージは既存どおり
- 取り消しは**直前に自分が作成した id のみ**を対象にし、失敗時は無言で終わらせずトーストで通知
- 新規の外部通信・新規ストレージ・写真の取り扱いは無し。**CSP 変更不要**
- 保存経路は既存の `withSaveTimeout` / `saveErrorMessage` 安全網に乗せる（CLAUDE.md の規律）

**LEGAL**
- カフェイン1行表示は §12 を維持: 「推定」「約◯mg」「個人差があります」を削らない。目標比較は中立表現（目標内／目標超え）のまま、断定ラベルを作らない
- 出典（食品安全委員会・EFSA）はカフェイン画面で継続提示。クイックシートの1行は**既存文言をそのまま流用**して差異を作らない
- 新UIに睡眠・健康への新たな言及を追加しない
- 盤の色名（ターコイズ／コバルト等）は一般名詞で商標上の問題なし。実在レーベルの名称・ロゴは使わない

### 5. 決定事項（2026-09-18 合意・いずれも推奨案を採用）

1. **抽出「完了」＝ターンテーブル計測の停止**。抽出中画面はターンテーブル計測を主に、蒸らしカウントダウンを重ねる。
   「抽出完了」操作で `totalTimeSec` を確定して Side B へ遷移する（**F-5 蒸らしタイマー値の自動転記もこれで満たす**）。
   蒸らし完了での自動遷移は採らない（注湯の途中で評価画面に飛ぶため）
2. **Side A/B はブロックの固定分類**。準備系＝Side A／評価系＝Side B に固定し、既存の `main`/`detail` は
   各面の中の「メイン／詳細」として維持、`hidden` は従来どおり。記録画面カスタマイズ（§7）の画面変更は最小に留める
3. **盤の色は「抽出中＋保存演出」に適用**。ホーム・記録詳細・オンボーディングの盤は現状色のまま
4. **保存後は演出の後にトースト**。星あり＝フル演出／星なし＝静かな演出を出し、終わってから
   「記録しました・取り消す」を5秒表示。節目演出は従来どおり

5. **盤の色はバックアップ対象外**（A）。`megroove-settings` は従来どおりバックアップに含めない＝就寝時間・目標mg と同じ扱い
6. **案Bのプリセットは最大3枚**（B）。「前回と同じ」＋よく使うレシピ上位2。レシピ未使用のユーザーは1枚のみ（従来の見え方）
7. **案A の Side A にも保存ボタンを残す**（C）。星だけ付けて保存したい人の往復を増やさない（§2 の最優先＝手軽さの保護）

---

*最終更新: 2026-09-26（記録フォームUI改善 P1〜P5・F-B・**F-4 までデプロイ済み＆確認済み**。
ROADMAP §2 短期の残りは F-7（「1年前の今日」のフォールバック）のみ）。状態が変わったらこのファイルを更新すること。*
